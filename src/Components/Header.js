import React from 'react';
import '../App.css';

const Header = () => {
    return (
        <header className="banner">
            <div>
                <span>AVL Test Tool</span>
            </div>
        </header>
    );
}

export default Header;